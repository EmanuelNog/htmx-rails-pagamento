<div class="container">
  <h1>Gerenciar Contas</h1>

  <div class="grid">
    <div class="card">
      <h2>Nova Conta</h2>
      <%= render 'form', conta: @conta %>
    </div>

    <div class="card">
      <h2>Contas Cadastradas</h2>
      <div id="contas-list">
        <%= render 'contas_list', contas: @contas %>
      </div>
    </div>
  </div>
</div>
